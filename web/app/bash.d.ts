/** Types for the build-free client helper in `bash.js`, so the check suite can import it. */
export function parseBang(line: string): string | undefined;
export function isBangMode(line: string): boolean;
